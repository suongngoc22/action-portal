{
  "name": "action-portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview",
    "sass:dev": "sass --watch --embed-source-map src/scss:styles",
    "sass:prod": "sass --no-source-map --style compressed src/scss:styles"
  },
  "devDependencies": {
    "sass": "^1.65.1",
    "vite": "^4.4.5"
  }
}
